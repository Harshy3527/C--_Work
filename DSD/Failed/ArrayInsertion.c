#include <stdio.h>
int main(){
    int N= 10, num, pos; 
    int arr[] = {2,1,7,6,0};
    printf("Enter the element to be inserted and the index it should be at : ");
    scanf("%d %d", num, pos);
    for(int i =pos+1; i<sizeof(arr)/4; i++){
        
    }
}